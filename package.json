{
  "name": "coverage-converter-simplecov",
  "version": "1.0.0",
  "description": "GitHub Action for converting Simplecov coverage reports to a simplified JSON format",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-jest": "^27.9.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5"
  }
}
